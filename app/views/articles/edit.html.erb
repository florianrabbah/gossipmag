<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.15/dist/tailwind.min.css" rel="stylesheet">
    <title>Formulaire DArticle</title>
</head>

<body class="bg-gray-100">
    <nav class="bg-purple-600 p-4 text-white">
        <div class="container mx-auto flex justify-between items-center">
            <a href="/" class="text-xl font-bold">Accueil</a>
        </div>
    </nav>

    <div class="container mx-auto flex justify-center items-center h-screen">
        <div class="bg-white p-8 rounded shadow-md w-96">
            <h1 class="text-2xl font-bold mb-6">Créer un nouvel article</h1>

            <%= form_with(model: @article, local: true) do |form| %>
                <div class="mb-4">
                    <%= form.label :title, class: "block text-gray-700 text-sm font-bold mb-2" %>
                    <%= form.text_field :title, class: "form-input mt-1 p-2 w-full rounded-md border border-gray-300" %>
                </div>

                <div class="mb-4">
                    <%= form.label :content, class: "block text-gray-700 text-sm font-bold mb-2" %>
                    <%= form.text_area :content, class: "form-textarea mt-1 p-2 w-full rounded-md border border-gray-300" %>
                </div>

                <div class="flex items-center justify-center mt-6">
                    <%= form.submit "Enregistrer", class: "bg-purple-600 hover:bg-purple-700 text-white font-bold py-2 px-4 rounded-md" %>
                </div>
            <% end %>
        </div>
    </div>
</body>

</html>
