
<div class="container mt-5">
  <h2>Membres du Club</h2>
  <ul>
    <% @club_memberships.each do |membership| %>
      <li>
        <%= link_to membership.user.name, user_path(membership.user) %>
      
      </li>
    <% end %>
  </ul>
</div>
